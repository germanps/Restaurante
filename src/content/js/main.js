/* Esta funcionalidad de añadir clase o quitarla está dispuesta en el mismo componente Lienzo */
/* window.addEventListener('load', () => {
   const links = document.querySelectorAll('.tab-list-item a');
   links.forEach( (el, index) => {
      el.addEventListener('click', (e) => {
         const links = document.querySelectorAll('.tab-list-item a');
         links.forEach(el => {
            el.className = "tab";
         });
         e.target.className = "tab active";
      })
   });
}, false); */